<UserControl x:Class="Furniture.Views.PlywoodView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Furniture.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d">
    <Grid VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
                  

        <!--Thickness-->
        <Slider Grid.Row="0" Padding="10"
                MinWidth="50" MaxWidth="150"
                VerticalAlignment="Center"
                Minimum="{Binding Path=Plywood.Min.Value}"
                Maximum="{Binding Path=Plywood.Max.Value}"
                Value="{Binding Path=SliderValue}"
                IsSnapToTickEnabled="True"
                TickFrequency="0.01"
                Ticks="{Binding Path=Ticks}" />

        <TextBlock Grid.Column="1" Padding="10" MinWidth="10"
                   Text="{Binding Path=Thickness, Mode=TwoWay}" />

        <!--Count-->
        <Grid Column="2" Margin="10">
            <TextBox Text="{Binding Path=QuantityField, 
                                    TargetNullValue='0',
                                    UpdateSourceTrigger=PropertyChanged}" />
        </Grid>
    </Grid>
</UserControl>